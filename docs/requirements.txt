In order to run this project you require the following

mesa>=1.6.0
solara
networkx
matplotlib
pandas

To install these dependencies, run this in powershell as an administrator within the project directory.
pip install -r requirements.txt
